# Python Learning

记录我的 Python 学习过程，通过练习逐步建立编程、数据处理和项目开发能力。

## Day 1

### 学习内容

- Python 变量、输入和输出
- `if / elif / else` 条件分支
- 列表 `list`
- `for` 循环
- 字符串的 `split()` 方法
- 列表的 `append()` 方法
- 类型转换：`int()`、`float()`
- f-string 格式化输出

### 完成的程序

- `main.py`：基础变量与输出练习
- `discount.py`：商品折扣计算器
- `score_list.py`：成绩统计分析程序

### 成绩分析程序功能

- 读取一行中的多个成绩
- 计算总分和平均分
- 统计最高分和最低分
- 统计各成绩等级人数

## Day 2

### 学习内容

* `for` 和 `while` 循环的区别
* 使用 `break` 结束整个循环
* 使用 `continue` 跳过本轮循环
* 使用 `range()` 控制循环范围和步长
* 使用 `range()` 正序、倒序遍历列表
* 使用 `enumerate()` 同时获取序号和元素
* 使用 `try-except ValueError` 处理非法输入
* 使用 `if not scores` 判断空列表
* 输入数据的类型检查和范围检查
* 手动计算总分、平均分、最高分和最低分

### 完成的程序

* `day02_loop.py`：成绩列表统计练习
* `day02_while.py`：循环录入成绩和输入验证
* `day02_score_manager.py`：完整成绩录入与统计程序
* `day02_range.py`：`range()`、倒序遍历和 `enumerate()` 练习

### 成绩管理程序功能

* 连续录入多个成绩
* 输入 `q` 结束录入
* 拒绝非整数输入
* 拒绝不在 0～100 范围内的成绩
* 处理没有录入成绩的情况
* 计算平均分、最高分和最低分
* 统计及格人数

## Day 3

### 学习内容

* 字典的创建、查询、修改和新增
* 使用 `items()` 遍历字典的键和值
* 使用嵌套字典保存多名学生的多科成绩
* 使用 `get()` 安全查询字典数据
* 使用 `zip()` 配对遍历多个列表
* 使用列表推导式筛选和转换数据
* 函数的定义、参数、默认参数和关键字参数
* 函数返回多个结果
* 可变对象作为函数参数时产生的副作用
* 使用 `copy()` 和 `deepcopy()` 复制数据
* 将函数拆分到模块中并使用 `import` 导入

### 完成的程序

* `circuit_statistics.py`：偶数筛选、求和与计数练习
* `student_report.py`：使用 `zip()` 生成学生及格情况
* `dictionary.py`：字典增删改查与遍历练习
* `list_comprehension.py`：列表推导式筛选和平方计算
* `student_subjects.py`：嵌套字典与学生多科成绩分析
* `function_side_effect.py`：函数修改列表及安全复制练习
* `copy_test.py`：嵌套数据的深拷贝练习
* `score_utils.py`：成绩分析函数模块
* `student_report_main.py`：导入模块并生成学生成绩报告

### 学生成绩分析程序功能

* 保存并更新多名学生的多科成绩
* 计算数学和英语的平均分
* 根据及格线判断成绩是否及格
* 支持自定义及格线
* 按学生输出各科成绩、平均分和结果
* 将成绩分析函数与主程序拆分到不同模块
